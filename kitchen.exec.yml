---
driver:
  name: exec

transport:
  name: exec

platforms:
  - name: localhost
  - name: windows-winrm
  - name: windows-fips-ssh
    transport:
      name: ssh
      username: <%= ENV['machine_user'] %>
      password: <%= ENV['machine_pass'] %>
