---
driver:
  name: exec

transport:
  name: exec

platforms:
  - name: localhost
  - name: windows
  transport:
    name: winrm
    elevated: true
  - name: win-fips-ssh
    transport:
      name: ssh
      username: <%= ENV['machine_user'] %>
      password: <%= ENV['machine_pass'] %>
