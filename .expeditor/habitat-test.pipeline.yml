---
expeditor:
  defaults:
    buildkite:
      timeout_in_minutes: 30
      retry:
        automatic:
          limit: 1

steps:

  - label: ":linux: Validate Habitat Builds of test-kitchen"
    commands:
      - sleep 60
    expeditor:
    executor:
      docker:
        image: ruby:3.1
        privileged: true
