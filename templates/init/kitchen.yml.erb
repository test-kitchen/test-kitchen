---
driver:
  name: <%= config[:driver_plugin] %>

provisioner:
  name: <%= config[:provisioner] %>

transport:
  name: <%= config[:transport] %>

platforms:
  - name: ubuntu-24.04
  - name: almalinux-10

suites:
  - name: default
    run_list:
<% config[:run_list].each do |recipe| -%>
      - <%= recipe %>
<% end -%>
    attributes:
