---
driver:
  name: <%= config[:driver_plugin] %>

provisioner:
  name: <%= config[:provisioner] %>

platforms:
  - name: ubuntu-20.04
  - name: centos-8

suites:
  - name: default
    run_list:
<% config[:run_list].each do |recipe| -%>
      - <%= recipe %>
<% end -%>
    attributes:
