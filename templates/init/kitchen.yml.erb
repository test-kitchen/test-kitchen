---
driver: <%= config[:driver_plugin] %>
provisioner: <%= config[:provisioner] %>

platforms:
  - name: ubuntu-12.04
  - name: centos-6.4

suites:
  - name: default
    run_list:
<% config[:run_list].each do |recipe| -%>
      - <%= recipe %>
<% end -%>
    attributes:
