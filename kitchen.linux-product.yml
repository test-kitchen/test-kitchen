---
driver:
  name: proxy
  host: localhost
  reset_command: "echo hello"
  port: <%= ENV["MACHINE_PORT"] %>
  username: <%= ENV["MACHINE_USER"] %>
  password: <%= ENV["MACHINE_PASS"] %>

platforms:
  - name: linux

verifier:
  name: busser
